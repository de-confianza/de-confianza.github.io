{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "gh-pages -d public",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.9.1",
    "vue": "^3.4.10",
    "vue-router": "^4.2.5"
  },
  "dependencies": {
    "gh-pages": "^6.1.1"
  }
}
